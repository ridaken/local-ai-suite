"""local-ai-suite MCP gateway — a passive tool provider for local AI clients.

The gateway advertises tool schemas and executes calls; the model (via the
client harness) decides which tools to call. See the design blueprint in
plans/ for the full architecture.
"""

__version__ = "0.1.0"
